# src

Source code utilities and reusable scripts can be placed here as the project evolves.

For this portfolio version, the primary analytical artifact is the notebook in `notebooks/meridian_prac.ipynb`, with summary outputs documented in `/docs` and `/reports`.
